# if in any folder __init__.py is created then the folder will be considered as package
# can only use relative path import in package
# from . import hello
from utils.hello import say_hello# need to use from directory chinanor jonno